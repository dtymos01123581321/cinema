//const baseURL = 'https://cinema.dtymos-cv.space/api';
const baseURL = 'http://localhost:6101/api';

const apiEndpoints = {
  login: `${baseURL}/users/login`,
  search: `${baseURL}/search`,
};

export default apiEndpoints;
